version = "1.0.0"
description = "backport of the +:[emote] reaction functionality with functionality to go up to 5 messages up to react using more pluses"

aliucord.changelog.set("""
    # 1.0.0
    * inital release
""".trimIndent())
